# 本地真实测试 Tasklist

生成日期：2026-07-08  
当前策略：先在本地完成真实链路测试，暂时不上服务器；服务器部署、应用端分发作为后续准备项纳入清单。

## 0. 当前确认结论

- 本地后端当前可通过 `http://localhost:8080` 访问，`http://127.0.0.1:8080` 在当前环境不稳定，桌面端本地测试优先配置为 `http://localhost:8080`。
- MySQL/MariaDB 与 Redis 已在本地/WSL 环境中运行。
- 当前后端连接库为 `private_domain_assistant_smoke`，默认账号 `admin/admin123` 可登录。
- 当前外部真实接口配置为空，后端当前仍适合 mock/半真实测试；要做真实接口测试，需要补齐外部配置。
- 现有接口里有图像识别接口、Skill/场景接口；已补入独立“思考 LLM / 对话生成 LLM”多环境配置、统一 runtime、五个业务场景路由、调用日志和主备降级，后续还需要接入真实 provider 和 A/B 效果评测。
- 已确认优先目标：先让当前已开发功能在本地动起来，能登录、能点击、能调用基础接口，再逐步接真实外部服务。
- 2026-07-10 已确认：侧边栏应用端本地基础体验真机测试成功；管理后台尚未进行用户侧手测。
- 已确认暂不做成本控制，其他遗漏项先纳入后续实现与验收范围。

## 0.0 本地启动记录

记录时间：2026-07-08 21:25

- [x] 后端健康接口可用：`GET http://localhost:8080/api/v1/auth/config` 返回 200。
- [x] 默认账号可登录：`admin/admin123`。
- [x] Vite 页面可用：`http://localhost:5173/` 返回 200。
- [x] 运营后台可登录，登录后能进入 Skill 场景管理页。
- [x] 桌面端 `npm run typecheck` 通过。
- [x] 桌面端 `npm run renderer:smoke` 通过。
- [x] 手工测试 readiness 通过：`.tools/acceptance/manual_test_readiness.json`。
- [x] 真实外部代码 readiness 通过：`.tools/external/real_external_readiness.json`。
- [x] 基础接口 smoke 通过：健康、配置、Skill、桌面状态、今日跟进、速搜、公告。
- [x] 已备份并清理 Electron 旧登录态缓存：`%APPDATA%/private-domain-assistant-desktop/.backup-20260708-212415`。
- [x] 已重新启动 Electron 桌面端。

当前本地登录方式：

- API 地址：`http://localhost:8080`
- 账号：`admin`
- 密码：`admin123`

如果侧边栏再次出现“点不动/没反应”，优先检查是否使用了旧 API 地址 `http://127.0.0.1:8080` 或旧 token。

## 0.1 已确认产品决策

- [x] 思考 LLM 与现有 Skill 并存：LLM 负责通用理解/生成/总结，Skill 负责业务场景编排和规则化流程。
- [x] 多 LLM 配置按场景选择，并保留全局默认模型。
- [x] LLM 调用日志本地测试可完整记录；正式环境默认脱敏记录。
- [x] 失败降级顺序：备用模型 -> 模板回复 -> 提示人工处理。
- [x] 测试数据先使用本地测试库，避免污染真实业务库。
- [x] 侧边栏正式交付优先考虑 Windows 安装包 + 版本检查 + 后端下载更新。
- [x] 当前阶段先跑 mock/基础链路基线，再接真实接口。

## 0.2 已补充遗漏项

- [ ] 隐私安全：客户手机号、聊天截图、LLM prompt、接口日志需要脱敏策略。
- [ ] API key 安全：外部服务 key 需要加密存储，避免写进前端或明文日志。
- [ ] 测试数据隔离：本地测试库、真实业务库、未来生产库要严格分开。
- [ ] 备份恢复：服务器上线前要有数据库备份、配置备份、安装包回滚。
- [ ] 监控告警：数据库、Redis、后端、外部接口、LLM 失败率、表格写入队列。
- [ ] 权限边界：管理员、组长、管家的后台与侧边栏权限需要专项测试。
- [ ] 桌面端系统权限：截图、剪贴板、企业微信窗口识别、杀毒误报、未签名安装包提示。
- [ ] 模型效果评测：同一批聊天样本跑多个 LLM，按质量、速度、稳定性打分。

## 1. 本地测试环境准备

- [ ] 统一本地启动方式：明确后端、数据库、Redis、桌面端 dev server 的启动命令。
- [ ] 固定本地后端地址：桌面端 `apiBaseUrl` 使用 `http://localhost:8080`。
- [ ] 检查数据库连接：确认连接库名、账号、Flyway 迁移、默认账号、基础配置表。
- [ ] 检查 Redis：确认登录 token、WebSocket、配置刷新、离线消息相关能力可用。
- [ ] 准备本地测试数据：客户档案、标签、跟进规则、速搜内容、账号角色、公告、审计数据。
- [ ] 整理本地测试账号：管理员、组长、管家至少各 1 个。

## 2. 外部接口配置准备

- [ ] 配置真实 Skill/场景接口：`skill.api_base_url`、`skill.api_key`。
- [ ] 配置真实图像识别接口：`image.api_base_url`、`image.api_key`、`image.model`。
- [ ] 配置真实企微表格接口：`table.api_base_url`、`table.api_key`。
- [x] 补充独立思考 LLM 配置入口：已支持 LLM provider 环境管理，并已接入回复生成、档案提取、跟进建议、异常识别、总结补位等业务链路。
- [x] 支持多 LLM 配置：允许同时维护多个 LLM provider，便于本地对比质量、速度、成本和稳定性。
- [x] 为每个 LLM 配置测试入口：可测试连通性、模型可用性、鉴权、超时和返回格式。
- [x] 为每个业务场景配置 LLM 路由：不同场景可选择不同模型，例如回复建议、档案提取、跟进策略、异常识别。
- [x] 增加 LLM 降级策略：主模型失败后可切备用模型，备用模型全失败后回退到原 Skill/模板/人工处理链路。

## 3. 思考 LLM 能力设计

- [x] 新增 LLM provider 配置表或复用 AI 配置中心扩展字段。
- [x] 每个 provider 至少支持：名称、base URL、API key、模型名、协议类型、是否启用、超时、温度、最大 token。
- [x] 支持 OpenAI-compatible 协议，后续可接入不同供应商。
- [x] 支持按场景绑定模型：`reply_generation`、`profile_extraction`、`followup_suggestion`、`abnormal_detection`、`summary`。
- [x] 后端新增统一 LLM client/service，避免各模块直接写供应商调用逻辑。
- [x] 管理后台新增多 LLM 配置与测试界面。
- [x] 侧边栏展示 LLM 异常状态：已接入配置缺失和近期调用失败率提醒；更细的错误码展示后续可继续增强。
- [x] 本地主备降级验收：受控 fake provider 已验证主模型失败、备用模型成功、调用统计记录主失败和备成功。
- [ ] 真实 provider 验收标准：至少能配置 2 个 LLM，并在同一条聊天样例上做 A/B 结果对比。

## 4. 侧边栏应用端本地测试

- [x] 侧边栏应用端本地基础体验：登录、工作台、客户档案、跟进列表、速搜、回复建议基础界面已由用户真机确认。
- [ ] 多角色登录与 token 刷新：管理员、组长、管家角色分别验证。
- [ ] WebSocket 连接：登录后在线状态、断线重连、过期 token 处理。
- [ ] 聊天识别：手动截图、剪贴板图片、图像识别接口真实调用。
- [ ] 回复建议：思考 LLM/Skill 接口生成建议、重新生成、选择建议、失败降级。
- [ ] 复制回填：复制建议内容、确认发送、发送后触发档案更新与跟进链路。
- [ ] 客户搜索：手机号、昵称、模糊匹配、无结果、重复客户。
- [ ] 客户档案：详情读取、手动编辑、待确认建议、批量确认/拒绝。
- [ ] 今日跟进：提醒列表、过期提醒、新客提醒、规则命中。
- [ ] 速搜：文本、图片、分类、关键词、复制使用。
- [ ] 保存到表格：新增客户、更新客户、失败重试、队列状态。
- [ ] 求助模式：管家发起、组长回复、WebSocket 推送、离线处理。
- [ ] 异常敏感提醒：敏感词、风险提示、历史记录。
- [ ] 工作台视图：今日概览、待处理事项、公告、健康状态。
- [ ] 离线可用：断网、后端不可用、恢复同步。

## 5. 管理后台本地测试

- [ ] Skill 场景管理：新增、编辑、启停、测试调用。
- [ ] AI 配置中心：图像识别、思考 LLM、多模型配置、模型测试、模型切换。
- [ ] 客户数据对接：数据源、字段映射、同步、导入、同步日志。
- [ ] 速搜内容管理：文本/图片内容、上传、启停、排序、删除。
- [ ] 账号与权限：管理员、组长、管家、停用、重置密码、权限边界。
- [ ] 跟进规则引擎：规则配置、启停、命中测试、提醒生成。
- [ ] 客户标签与分层：标签分类、标签值、启停、删除约束。
- [ ] 运营分析看板：概览、漏斗、员工、来源、阶段、风险。
- [ ] 版本管理：版本记录、上传包、发布、撤回、桌面端检查更新。
- [ ] 系统公告：新建、发布、停止、桌面端展示。
- [ ] 审计日志：筛选、导出、下载、权限控制。
- [ ] 系统健康监控：数据库、Redis、外部接口、队列、WebSocket。

## 6. 本地自动化验收

- [x] 后端 API 验收：`scripts/acceptance_backend_api.py --no-start`，168/168 通过。
- [x] 后端 API 验收质量检查：`scripts/verify_backend_api_acceptance_quality.py`。
- [x] 手工测试就绪检查：`scripts/verify_manual_test_readiness.py --frontend-url http://localhost:5173/ --backend-url http://localhost:8080`。
- [x] API 映射覆盖检查：`scripts/verify_api_mapping_coverage.py`，114/114 覆盖。
- [x] Controller 测试覆盖审计：`scripts/verify_controller_test_coverage.py`，19/19 覆盖。
- [x] 数据库结构对齐：`scripts/verify_database_alignment.py`。
- [x] 枚举契约对齐：`scripts/verify_enum_contract_alignment.py`。
- [x] 管理后台产品面覆盖：`scripts/verify_admin_product_surface.py`。
- [x] 桌面端类型检查：`npm run typecheck`。
- [x] 桌面端单测：`npm run test`，32 个测试文件、204 个用例通过。
- [x] Renderer smoke：`npm run renderer:smoke`，覆盖 Electron 侧边栏桥接、工作台、客户档案跳转、速搜抽屉、置顶按钮和后台入口。
- [x] 运营后台页面级 smoke：浏览器重新登录后，12 个主要分组均能加载，无 `Failed to fetch`、401/403。
- [x] 真实外部接口 readiness：`scripts/verify_real_external_readiness.py`。
- [x] 本地 runtime readiness 已纳入五个 LLM 场景配置行检测：`scripts/verify_local_runtime_readiness.py`。
- [x] 生产阻塞项审计：`scripts/verify_production_blockers.py`。

当前剩余生产阻断：

- [ ] 真实外部供应商验收：Skill、图像识别、企微表格需要接入真实 key 后再验收。
- [ ] 正式发布包签名：上线安装包需要配置代码签名证书。

## 7. 服务器部署准备项

- [ ] 明确服务器形态：云服务器、内网服务器、还是公司自有服务器。
- [ ] 服务器需要部署：Spring Boot 后端、MySQL/MariaDB、Redis、Nginx/HTTPS、上传/下载文件目录、日志目录、备份任务。
- [ ] 准备域名与 HTTPS 证书。
- [ ] 准备生产环境变量：数据库账号、Redis 地址、JWT 密钥、密钥加密 key、外部接口 key。
- [ ] 准备数据库备份和恢复策略。
- [ ] 准备日志轮转和错误告警。
- [ ] 准备外部接口访问白名单或固定出口 IP。
- [ ] 准备生产配置与本地配置隔离方案。
- [ ] 准备灰度/回滚方案。
- [ ] 准备管理员初始化流程。

## 8. 侧边栏应用端交付方式

- [ ] 明确侧边栏是 Electron 桌面应用，不是直接部署到服务器给用户打开网页。
- [ ] 服务器提供后端 API、WebSocket、版本检查、安装包下载。
- [ ] 桌面端需要打包成 Windows 安装包或免安装目录包。
- [ ] 桌面端配置生产 API 地址，例如 `https://api.example.com`。
- [ ] 桌面端需要处理自动更新或手动下载更新。
- [ ] 桌面端需要签名策略：未签名包会有系统安全提示，正式发布建议代码签名。
- [ ] 本地测试阶段可以直接运行 Electron dev/preview；上线阶段需要生成发布包。
- [ ] 管理后台可以先内嵌在桌面端，也可以后续拆成独立 Web 后台。

## 9. 阶段里程碑

- [x] M0：当前已开发功能先能动起来，完成登录、侧边栏、后台基础点击和基础接口调用。
- [x] M1：本地 mock 链路全通，数据库与桌面端连接稳定。
- [ ] M2：本地真实图像识别、真实 Skill、真实表格接口接通。
- [x] M3：思考 LLM 多模型配置入口、统一 runtime、五个业务场景路由和本地主备降级验收已完成。
- [ ] M3.1：真实 LLM provider 接入和 A/B 效果测试。
- [x] M4：侧边栏应用端基础流程已自动化跑通。
- [x] M4.1：侧边栏应用端本地基础体验已真机确认。
- [ ] M4.2：侧边栏真实外部接口流程确认。
- [x] M5：管理后台基础写入类流程已自动化跑通。
- [x] M5.1：管理后台浏览器预检已通过，12 个模块可加载。
- [ ] M5.2：管理后台用户侧手测、真实 provider 配置和业务体验确认。
- [x] M6：自动化验收与手工验收报告初版完成。
- [ ] M6.1：真实 provider 和管理后台用户体验验收补齐。
- [x] M7：服务器部署方案、桌面端发布方案、生产配置清单已完成初版。
- [x] M7.1：生成 Nginx/systemd/env/备份脚本模板。
# 2026-07-09 更新：断点 025 已完成

- [x] LLM 场景路由后端基础结构已完成：可按 `REPLY_GENERATION`、`PROFILE_EXTRACTION`、`FOLLOWUP_SUGGESTION`、`ABNORMAL_DETECTION`、`SUMMARY` 维护模型路由。
- [x] LLM 调用日志表和统计查询已完成：后续业务接入后可记录模型、场景、耗时、成功率和错误信息。
- [x] 本地数据库已迁移到 `V60__llm_routes_and_call_logs.sql`。
- [x] 管理后台已补 LLM 路由配置 UI 和调用统计 UI。
- [x] 回复建议已接入 `REPLY_GENERATION`，并保留 Skill/模板兜底。
- [x] 客户档案提取已接入 `PROFILE_EXTRACTION` LLM 可选链路，默认关闭并保留 Skill 回落。
- [x] 跟进建议已接入 `FOLLOWUP_SUGGESTION` LLM 补位链路，默认关闭。
- [x] 异常识别已接入 `ABNORMAL_DETECTION` LLM 异步提醒链路，默认关闭。
- [x] 总结已接入 `SUMMARY` LLM 补位链路，默认关闭。
# 2026-07-09 更新：断点 026 已完成

- [x] 回复建议后端已接入 `REPLY_GENERATION` LLM 可选链路。
- [x] 默认仍走 Skill，避免未配置真实 LLM key 时阻断当前功能。
- [x] LLM 失败或返回格式异常时自动回落到 Skill。
- [x] 新增 `llm.reply_generation.*` 配置，数据库已迁移到 V61。
- [x] 管理后台已暴露 LLM 回复生成开关、prompt 和路由配置。
- [x] 侧边栏后续需要展示当前回复来源：LLM / Skill / 兜底。

# 2026-07-09 更新：断点 027 已完成

- [x] 管理后台配置中心已新增 `LLM 回复生成` 面板。
- [x] 管理后台配置中心已新增 `LLM 场景路由` 面板。
- [x] 管理后台配置中心已新增 `LLM 调用统计` 面板。
- [x] LLM 路由可新增、编辑、启停、删除，保存后自动刷新。
- [x] LLM 统计可按 7/14/30 天查看调用量、成功率和响应时间。
- [x] 侧边栏后续需要展示当前回复来源：LLM / Skill / 兜底。
- [ ] 真实 LLM provider 配置仍需后续提供后才能做真实模型效果测试。

# 2026-07-09 更新：断点 028 已完成

- [x] 后端回复接口已返回 `replySource`。
- [x] 侧边栏回复助手已展示 `LLM 生成`、`Skill 生成`、`系统兜底`。
- [x] 兜底状态不会误显示为 Skill 正常生成。
- [x] 侧边栏健康/提醒中心已展示 LLM 配置缺失和近期调用失败率提醒。

# 2026-07-09 更新：断点 029 已完成

- [x] `/api/v1/desktop/status` 已返回 `llmStatus`。
- [x] 侧边栏提醒中心已显示 LLM 配置缺失提醒。
- [x] 配置完整后已根据近 24 小时回复生成调用日志判断失败率过高。
- [x] 默认未启用 LLM 回复生成时不提醒，不影响当前本地测试。
- [x] 客户档案提取已接入 `PROFILE_EXTRACTION`。
- [x] 跟进建议、异常识别和总结均已接入对应 LLM 场景，默认关闭。

# 2026-07-09 更新：断点 030 已完成

- [x] 客户档案提取已接入 `PROFILE_EXTRACTION` LLM 可选链路。
- [x] 管理后台配置中心已新增 `LLM 档案提取` 面板。
- [x] 默认关闭 LLM 档案提取，避免没有真实 key 时影响本地测试。
- [x] LLM 失败默认回落原 Skill 档案提取。
- [x] `FOLLOWUP_SUGGESTION` 已接入发送确认补位链路。
- [x] `ABNORMAL_DETECTION` 已接入发送确认后的异步提醒链路。
- [x] `SUMMARY` 已接入发送确认摘要补位链路。

# 2026-07-09 更新：断点 031 已完成

- [x] 跟进建议已接入 `FOLLOWUP_SUGGESTION` LLM 补位链路。
- [x] 管理后台配置中心已新增 `LLM 跟进建议` 面板。
- [x] 默认关闭 LLM 跟进建议，避免没有真实 key 时影响本地测试。
- [x] 已有 Skill/前端跟进建议时不会重复调用 LLM。
- [x] `ABNORMAL_DETECTION` 已接入发送确认后的异步提醒链路。
- [x] `SUMMARY` 已接入发送确认摘要补位链路。

# 2026-07-09 更新：断点 032 已完成

- [x] 异常识别已接入 `ABNORMAL_DETECTION` LLM 场景。
- [x] 管理后台配置中心已新增 `LLM 异常识别` 面板。
- [x] 默认关闭 LLM 异常识别，避免没有真实模型评估前误报。
- [x] 命中后复用现有 `ABNORMAL_ALERT` 推送到侧边栏提醒中心。
- [x] `SUMMARY` 已接入发送确认摘要补位链路。

# 2026-07-09 更新：断点 033 已完成

- [x] 总结补位已接入 `SUMMARY` LLM 场景。
- [x] 管理后台配置中心已新增 `LLM 总结补位` 面板。
- [x] 默认关闭 LLM 总结补位，避免没有真实 provider 时影响本地测试。
- [x] 前端已提供 `conversationSummary` 时不会重复调用 LLM。
- [x] LLM 未启用、失败或返回为空时回落到已发送文本，不阻断发送确认。
- [ ] 真实 LLM provider 配置仍需后续提供后才能做真实模型效果测试。

# 2026-07-09 更新：断点 034 已完成

- [x] 本地 readiness 已纳入 LLM 场景配置行、场景开关和场景路由检测。
- [x] 真实外部代码 readiness 已纳入 LLM HTTP client、场景路由、调用日志和配置键检查。
- [x] 本地后端已重启，数据库迁移已补齐 V62-V65 LLM 场景配置。
- [x] 普通本地 readiness 通过，不要求真实 key。
- [ ] 强制真实外部 readiness 仍缺 `llm.api_base_url`、`llm.api_key`、`llm.model`、`table.api_base_url`、`table.api_key`。

# 2026-07-09 更新：断点 035 已完成

- [x] 重启迁移后侧边栏 renderer smoke 通过。
- [x] 管理后台产品面检查通过。
- [x] 手工 readiness 通过，当前本地前后端可登录、可访问。
- [x] 管理后台与主应用前端测试通过。
- [x] 桌面端类型检查通过。
- [x] 本地 runtime readiness 通过。
- [ ] 真实 LLM provider 与企微表格配置仍待提供后联调。

# 2026-07-09 更新：断点 036 已完成

- [x] LLM 路由运行时已支持多候选备用模型。
- [x] 主模型失败后会按优先级尝试备用模型。
- [x] 每次 LLM 尝试都会写入调用日志，便于后续查看主备命中和失败原因。
- [x] 所有 LLM 候选失败后仍回落原业务兜底链路。
- [ ] 真实 LLM provider 提供后，需要配置至少两个 LLM environment 验证主备切换。

# 2026-07-09 更新：断点 038 已完成

- [x] 已新增 LLM 主备切换受控自动验收脚本。
- [x] 已自动验证主 LLM 失败、备用 LLM 成功。
- [x] 已验证回复来源为 `LLM`。
- [x] 已验证 LLM 调用统计记录主失败和备成功。
- [ ] 真实 LLM provider 提供后，再做 live 主备切换验收。

# 2026-07-09 更新：断点 039 已完成

- [x] 手测批次已同步到断点 038 后状态。
- [x] A6 回复建议测试已纳入推荐回复首屏、当前任务、待处理队列和来源标签。
- [x] B1 AI/Skill 配置已纳入 LLM 环境、场景路由和调用统计。
- [x] C2 思考 LLM 已纳入真实 provider 后的五场景、主备、A/B 和统计验收。
- [x] 文档已移除“LLM 多模型仍未实现”的旧口径。
- [ ] 下一步可继续补本地手测报告模板，或整理服务器/发布准备清单。

# 2026-07-09 更新：断点 040 已完成

- [x] 已新增本地手测报告模板：`dev-progress/local_real_test_manual_report.md`。
- [x] 报告模板已覆盖侧边栏、管理后台、真实外部接口、服务器与发布准备。
- [x] 历史已修复问题已进入问题台账。
- [x] 当前真实 provider 和服务器信息阻塞项已单独列出。
- [ ] 下一步可执行批次 A 侧边栏手测，或整理服务器/发布准备清单。

# 2026-07-09 更新：断点 041 已完成

- [x] 已新增服务器部署与桌面端发布准备清单：`dev-progress/server_release_preparation_checklist.md`。
- [x] 已明确服务器承载范围和 Electron 侧边栏交付方式。
- [x] 已整理生产环境变量、Nginx 代理、备份、版本下载、签名 readiness 和生产阻塞检查。
- [x] 已列出需要用户后续决策/提供的服务器、域名、证书、provider 白名单和代码签名事项。
- [ ] 下一步可生成 Nginx/systemd/env/备份脚本模板，或执行批次 A 侧边栏手测。

# 2026-07-09 更新：断点 042 已完成

- [x] 已新增部署模板目录：`dev-progress/deploy_templates/`。
- [x] 已新增生产 `.env`、Nginx、systemd、数据库备份/恢复、桌面端发布步骤模板。
- [ ] 下一步可执行批次 A 侧边栏手测并填写 `local_real_test_manual_report.md`。

# 2026-07-09 更新：断点 043 已完成

- [x] 已新增侧边栏批次 A 自动化验收脚本：`scripts/acceptance_sidebar_batch_a.py`。
- [x] 批次 A 验收通过：`passed=true checks=14/14`。
- [x] 手测报告已更新 A1-A6 状态。
- [x] 侧边栏关键组件测试和 renderer smoke 已通过。
- [ ] 下一步可执行批次 B 管理后台写入类复测。

# 2026-07-09 更新：断点 044 已完成

- [x] 已新增管理后台批次 B 自动化验收脚本：`scripts/acceptance_admin_batch_b.py`。
- [x] 批次 B 验收通过：`passed=true checks=39/39`。
- [x] 手测报告已更新 B1-B5 状态。
- [x] 管理后台组件测试和产品面检查已通过。
- [ ] 下一步可补真实 provider 填写表和上线前最终验收表。

# 2026-07-09 更新：断点 045 已完成

- [x] 已新增真实 provider 配置填写表：`dev-progress/real_provider_config_form.md`。
- [x] 已新增上线前最终验收表：`dev-progress/final_acceptance_checklist.md`。
- [x] A/B 批次和最终验收入口已形成闭环。
- [ ] 下一步等待真实 provider 或管理后台手测反馈。

# 2026-07-10 更新：断点 046 已完成

- [x] 侧边栏应用端本地基础体验已由用户真机确认通过。
- [x] 手测报告已把 A1-A6 从“待真机确认”更新为“本地基础体验通过”。
- [x] 管理后台没有提前标记为用户手测通过，仍保持“自动化通过、待管理后台手测确认”。
- [ ] 下一步执行批次 B：管理后台用户侧手测。

# 2026-07-10 更新：断点 047 已完成

- [x] 管理后台旧登录态已退出，并使用 `admin/admin123` 重新登录成功。
- [x] 管理后台显示账号 `System Admin`，后端请求恢复正常。
- [x] 浏览器预检确认 12 个模块均可加载：Skill 场景管理、配置中心、客户数据对接、速搜内容管理、账号与权限、跟进规则、标签与分层、运营分析、版本管理、系统公告、审计日志、健康监控。
- [x] 未发现 `Failed to fetch`、401、403。
- [x] 已输出测试夹具盘点：`.tools/acceptance/admin_fixture_inventory.json`。
- [ ] 下一步执行管理后台用户侧手测；如需要，可先清理测试夹具。
