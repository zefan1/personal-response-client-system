# 本地真实测试批量手测清单

生成日期：2026-07-09  
当前阶段：本地 mock/基础链路已通过自动化验收，LLM 多模型与主备降级链路已完成本地受控验收，开始批量手工点测。

## 0. 手测前状态

- 后端地址：`http://localhost:8080`
- 前端/Vite：`http://localhost:5173`
- 推荐登录：`admin/admin123`
- 当前测试库：`private_domain_assistant_smoke`
- 推荐侧边栏 API 地址：`http://localhost:8080`
- 不推荐本机 Windows 侧使用：`http://127.0.0.1:8080`

已完成自动化基线：

- 后端接口验收：168/168 通过。
- API 映射覆盖：114/114 通过。
- Controller 覆盖：19/19 通过。
- 桌面端类型检查通过。
- 桌面端单测：32 个测试文件、204 个用例通过。
- Electron 侧边栏 renderer smoke 通过，覆盖桌面桥接、工作台、客户档案跳转、速搜抽屉、置顶按钮和后台入口。
- 运营后台 12 个主要分组已完成浏览器页面级 smoke，重新登录后均能加载数据或正常空状态。
- 数据库结构、枚举契约、后台产品面覆盖均通过。
- LLM 五个业务场景配置已落库并纳入 readiness：回复生成、档案提取、跟进建议、异常识别、总结补位。
- LLM 主备切换受控验收通过：主模型失败后备用模型成功，调用统计能记录主失败和备成功。

注意：

- 如果后台打开后显示账号为“当前账号”，并且页面提示 `Failed to fetch`，这是旧登录态/旧 token 的典型现象。先点“退出”，再用 `admin/admin123` 重新登录。
- 如果侧边栏仍显示旧的客户档案“加载超时”，刷新或重新打开侧边栏，让前端拿到包含 `phoneFull` 的新接口响应。
- 当前真实 LLM provider 和企微表格接口尚未配置；本地可先测 UI、配置保存、来源标签、降级和受控主备，不把真实模型质量作为本批失败项。

## 1. 批次 A：侧边栏应用端基础可操作性

目标：确认用户当前最关心的“应用端不是死的，可以点、可以跳、可以加载数据”。

### A1. 登录与首页状态

- [ ] 打开侧边栏或 Electron 桌面端。
- [ ] API 地址使用 `http://localhost:8080`。
- [ ] 使用 `admin/admin123` 登录成功。
- [ ] 登录后没有持续弹出接口连接失败。
- [ ] 工作台能显示今日跟进、公告或健康状态中的至少一种真实数据。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### A2. 工作台到客户档案

- [ ] 在工作台找到 `local-smoke-overdue` 或手机号展示为 `188****1111` 的跟进卡片。
- [ ] 点击“查看”。
- [ ] 页面应切到“客户档案”。
- [ ] 应直接显示该客户档案，而不是只停留在搜索框。
- [ ] 不应再出现“加载超时，请重试”。
- [ ] 手机号可展示为脱敏号，但操作请求应能正常加载真实客户。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### A3. 客户档案搜索与编辑

- [ ] 刷新或重开侧边栏，进入“客户档案”。
- [ ] 在客户档案搜索 `1111`，预期能自动打开 `逾期跟进客户 / 188****1111`。
- [ ] 再搜索 `18800001111`，预期同样能打开该客户档案。
- [ ] 打开详情后能看到中文样本字段：上海门店、产后修复、腹直肌分离和腰背酸痛、跟进中。
- [ ] 点击右上角 `↻` 图标刷新，按钮不应再超出侧边栏范围。
- [ ] 点击“编辑档案”，修改一个低风险字段，例如“担忧点”或“跟进历史”。
- [ ] 点击“保存”，预期出现“档案已保存”或表格同步提示。
- [ ] 不修改任何字段再次保存，预期提示“没有改动需要保存”。
- [ ] 保存后点击 `↻` 刷新，修改结果仍保留。
- [ ] 如有待确认建议，测试确认或拒绝其中一条。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### A4. 跟进列表

- [ ] 点击侧边栏“批量”入口，打开“今日跟进/跟进列表”面板。
- [ ] 顶部应显示待办客户数量，且至少能看到本地样本跟进。
- [ ] 切换“逾期跟进”，预期能看到 `逾期跟进客户 / 188****1111`。
- [ ] 点击该客户所在行的主体区域，预期跳转到客户档案并打开 `18800001111` 对应档案。
- [ ] 回到跟进列表，切换“今日待跟进”，预期能看到 `今日待跟进客户 / 188****2222`。
- [ ] 切换“今日预约”，预期能看到 `今日预约客户 / 188****3333`，或显示合理空状态。
- [ ] 勾选一条或多条跟进，底部出现批量栏。
- [ ] 点击“全选/反选”状态变化正常。
- [ ] 点击“批量发模板”，预期能打开模板选择或进入合理的下一步，不报错。
- [ ] 不出现“提醒服务持续不可用”或持续加载不结束。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### A5. 速搜

- [ ] 点击侧边栏“模板”入口，打开速搜面板。
- [ ] 搜索 `本地测试`，预期看到两条中文模板：`本地测试 团购开场白`、`本地测试 到店提醒`。
- [ ] 搜索 `local`，预期也能通过快捷码看到 `local_tuan_open` 与 `local_arrival_reminder`。
- [ ] 切换筛选“团购”，预期保留团购开场白；切回“全部”两条都可见。
- [ ] 点击某条模板的“复制”，预期提示“已复制”，剪贴板内容为对应模板文本。
- [ ] 搜索一个不存在的词，例如 `zzzz-no-result`，预期显示正常空状态，不报错。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### A6. 回复建议基础链路

- [ ] 方式一：在客户档案打开 `18800001111` 后，点击“生成回复”。
- [ ] 预期会切到或联动“回复助手/回复建议”区域，首屏展示“推荐回复”，下方仍保留当前任务和待处理队列。
- [ ] 方式二：点击侧边栏“回复助手”，在输入区填入样例：`客户说：我产后三个月，腹直肌分离，想先了解价格和到店评估。`
- [ ] 点击生成/重新生成，预期返回建议或明确展示 `LLM 未配置 / Skill 不可用 / 系统兜底`，不是页面无反应。
- [ ] 检查回复来源标签，预期能区分 `LLM 生成`、`Skill 生成`、`系统兜底`。
- [ ] 点击“重新生成”，预期按钮进入加载态后恢复，并出现新结果或合理错误。
- [ ] 点击“复制”或“确认发送”类按钮，预期不报错；如有发送确认，应能回写客户档案跟进记录或给出成功反馈。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

## 2. 批次 B：管理后台基础可操作性

目标：确认后台每个主要管理页都不是静态壳，能真实读写后端。

已自动完成：

- [x] 后台 12 个主要分组入口可点击、可加载，未出现 `Failed to fetch` 或 401/403。
- [x] Skill 场景管理、配置中心、客户数据对接、速搜内容、账号权限、规则、标签、看板、版本、公告、审计、健康均有页面级 smoke 记录。

用户本批重点：

- 主要验证“新增/编辑/启停/删除/导出/下载”等写入类交互是否符合你的业务预期。
- 不需要再逐个确认页面是否能打开；这部分机器已经先测过。

### B1. AI/Skill 配置

- [ ] 进入 Skill 场景管理。
- [ ] 列表能加载。
- [ ] 新增一条测试 Skill 配置。
- [ ] 编辑、启停、删除能正常完成。
- [ ] 进入 AI 配置中心，确认图像识别配置页能加载。
- [ ] 确认 LLM 环境列表能加载，支持新增、编辑、启用和测试连接。
- [ ] 确认 LLM 场景路由能配置 `REPLY_GENERATION`、`PROFILE_EXTRACTION`、`FOLLOWUP_SUGGESTION`、`ABNORMAL_DETECTION`、`SUMMARY`。
- [ ] 确认 LLM 调用统计能加载；没有真实调用时可以是 0 或空状态。
- [ ] 没有真实 LLM key 时，测试连接失败或业务走兜底属于预期，不作为本批失败。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### B2. 客户数据对接

- [ ] 进入数据源管理。
- [ ] 新增测试数据源。
- [ ] 配置字段映射。
- [ ] 执行导入或同步测试。
- [ ] 查看同步日志。
- [ ] 删除测试数据源。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### B3. 速搜内容管理

- [ ] 进入速搜内容管理。
- [ ] 搜索 `local` 能看到本地测试内容。
- [ ] 新增一条文本模板。
- [ ] 修改标题、快捷码、排序。
- [ ] 启停该模板。
- [ ] 删除该模板。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### B4. 账号权限

- [ ] 进入账号管理。
- [ ] 新增一个组长账号。
- [ ] 新增一个管家账号并绑定组长。
- [ ] 停用、启用、重置密码可用。
- [ ] 用非管理员角色登录，确认管理后台权限边界合理。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

### B5. 规则、标签、公告、审计、健康

- [ ] 跟进规则：新增、编辑、启停、删除。
- [ ] 标签管理：新增分类、标签值、启停、删除。
- [ ] 系统公告：新建立即公告，侧边栏能看到；停止后不再展示。
- [ ] 审计日志：能筛选、能导出、能下载。
- [ ] 健康监控：数据库、Redis、后端运行状态展示正常。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

## 3. 批次 C：真实外部接口接入后手测

目标：接入真实 key 后，确认图像识别、思考 LLM、Skill、表格接口能真实工作。

### C1. 图像识别

- [ ] 配置真实图像识别 base URL、API key、模型名。
- [ ] 上传或粘贴一张脱敏聊天截图。
- [ ] 能识别昵称、手机号、聊天文本、时间等关键字段。
- [ ] 识别失败时错误提示清楚，不泄露 key。

### C2. 思考 LLM

- [ ] 配置至少 1 个 LLM provider。
- [ ] 推荐配置 2 个 provider 或 2 个模型，用于主备切换和 A/B 测试。
- [ ] 为五个场景逐个配置路由：回复生成、档案提取、跟进建议、异常识别、总结补位。
- [ ] 先只打开 `LLM 回复生成`，使用同一条聊天文本分别生成回复建议。
- [ ] 对比质量、速度、失败率和来源标签。
- [ ] 查看 `LLM 调用统计` 是否记录模型、场景、耗时、成功率和错误原因。
- [ ] 验证主模型失败时能走备用模型；备用也失败时能走 Skill/模板/人工处理兜底。

### C3. Skill/场景接口

- [ ] 配置真实 Skill base URL 与 API key。
- [ ] 测试回复建议、重新生成、阶段建议等场景。
- [ ] 查看调用日志、错误码、耗时。
- [ ] 验证接口超时和鉴权失败提示。

### C4. 企微表格

- [ ] 配置真实表格接口。
- [ ] 使用测试表格，不污染正式业务表。
- [ ] 新客保存到表格成功。
- [ ] 老客更新字段成功。
- [ ] 写入失败时能重试或保留失败状态。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

## 4. 批次 D：服务器与发布准备

目标：本地功能稳定后，为上线部署和侧边栏交付做准备。

- [ ] 确认服务器类型、系统、访问方式。
- [ ] 确认域名和 HTTPS 证书。
- [ ] 准备 MySQL/MariaDB、Redis、Nginx、日志目录、上传目录、备份目录。
- [ ] 准备生产环境变量：数据库、Redis、JWT、密钥加密 key、外部接口 key。
- [ ] 确认外部接口 IP 白名单或服务器出口 IP。
- [ ] 准备 Windows 桌面端打包方式。
- [ ] 准备正式 API 地址写入桌面端。
- [ ] 决定是否配置代码签名证书。
- [ ] 验证版本检查、安装包下载、更新提示。

记录：

- 结果：
- 问题截图：
- 是否形成新断点：

## 5. 当前不作为本地失败的事项

- 真实外部供应商未验收：需要用户提供真实 key 和测试样本后执行。
- 正式发布包未签名：这是上线发布要求，不影响当前本地功能测试。
- 真实 LLM provider 未提供：不影响当前本地 mock、配置、降级和受控主备验收。
- 真实企微表格接口未提供：不影响当前本地客户档案读写和后台配置验收。
