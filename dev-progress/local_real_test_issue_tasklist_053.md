# 本地真机问题修复 Tasklist 053

时间：2026-07-13
范围：管理后台、侧边栏、账号安全、客户查询、提醒展示、速搜变量

## 生产级约束

- [x] 每项修改追踪前端、API、Service、Repository、数据库和 WebSocket 消费链路。
- [x] 数据库结构以真实 migration/表结构为准，不凭前端模型推断。
- [x] 新旧数据保持兼容，不依赖手工补数据或一次性脚本。
- [x] 同模块列表、详情、创建、编辑、删除、筛选、分页和统计入口完成回归。
- [x] 后端返回保持完整正确，前端容错不掩盖后端缺陷。

## 1. 密码重置后侧边栏立即退出

- [x] 后端密码重置、账号停用、删除、角色/直属关系安全变更后推送定向会话失效消息。
- [x] WebSocket 在线推送和离线队列行为明确，失效消息不在新登录后误重放。
- [x] 侧边栏收到失效消息后立即清除 token 并返回登录页，不等待下一次 API 请求。
- [x] 菜单切换、工作台、客户档案、回复助手等入口统一失效。
- [x] 覆盖后端 Service/WS 测试和前端 App/消息总线测试。

## 2. 账号表格操作列重新排版

- [x] 状态后新增稳定的“操作”列。
- [x] 编辑、重置密码、启停、删除按钮不再出现在姓名下方。
- [x] 宽屏、窄屏和长姓名/长组长名不重叠。
- [x] 账号筛选、分页、创建、编辑、删除和重置密码回归。

## 3. 管理后台新增客户搜索入口

- [x] 核对 customers 表真实字段和现有客户搜索 Service/权限隔离规则。
- [x] 新增管理员专用客户查询 API，ADMIN 可查全量，不能破坏管家数据隔离。
- [x] 管理后台提供明确的“客户查询”区域，可搜索姓名/昵称、手机号、门店、项目、来源、管家和来源行标识等真实字段。
- [x] 搜索 `1111` 时展示匹配客户摘要，并可查看关键档案字段。
- [x] 覆盖空关键字、无结果、多结果、分页/限制和中文错误。

## 4. 侧边栏提醒弹层展示修复

- [x] 修复提醒弹层被侧边栏容器裁切、内容不可见或层级错误。
- [x] 提醒列表可查看标题、正文、时间、等级和已读状态。
- [x] 点击提醒、关闭弹层、清除历史和未读数量回归。
- [x] 工作台公告/提醒推送链路保持可用。

## 5. 速搜变量中文化与兼容替换

- [x] 所有变量按钮使用中文业务名称，不再混用英文键名。
- [x] 插入正文时显示中文占位符，管理人员所见与按钮一致。
- [x] 侧边栏替换引擎同时支持旧英文占位符和新中文占位符。
- [x] 客户昵称、手机号、门店、项目、阶段、意向等级、跟进时间、预约、到店、分配管家全部覆盖。
- [x] 新建、编辑、预览、复制、变量缺失和旧数据兼容测试通过。

## 交付验证

- [x] Java 相关单测通过。
- [x] Vue/Vitest 相关单测通过。
- [x] `npm run typecheck` 通过。
- [x] 前后端重启后本地真实操作验证通过。
- [x] 新增断点记录，列出已完成项和剩余项。

## 验收记录

- Java 相关回归：29/29 通过。
- Vue/Vitest 全量回归：35 个测试文件、232/232 通过。
- TypeScript 类型检查和生产构建通过。
- 数据库结构对齐检查：34 张表，933 个 Repository 列引用，0 违规。
- 管理后台真实 API 批量验收：39/39 通过。
- 客户查询真实 API：`1111` 命中 1 条；空关键词分页返回 6 条；非法分页返回中文错误。
- 桌面端 renderer smoke：通过，包含提醒弹层几何与内容可见性检查。
- 管理后台 renderer smoke：通过，包含客户查询、账号 7 列布局和中文变量插入检查。
- 断点文件：`dev-progress/local_real_test_breakpoint_053.md`。
