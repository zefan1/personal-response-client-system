# 断点 041：服务器部署与桌面端发布准备清单

时间：2026-07-09 23:15  
状态：已完成

本轮目标：

- 在继续真实接口或上线前，先把服务器需要放什么、桌面端怎么处理、生产前还缺什么写清楚。
- 当前仍不部署服务器，只做上线准备文档。

处理结果：

- 新增 `dev-progress/server_release_preparation_checklist.md`。
- 清单已覆盖：
  - 推荐服务器组成。
  - 目录规划。
  - 生产环境变量。
  - Nginx 反向代理要点。
  - 数据库与备份。
  - 桌面端发布方式。
  - 桌面端生产 API 地址。
  - 版本管理与安装包下载。
  - 上线前验收命令。
  - 需要用户决策或提供的事项。
  - Codex 可继续完成的事项。
- 已明确侧边栏是 Electron 桌面应用，服务器提供 API/WebSocket/版本检查/安装包下载。
- 已明确生产前必须替换默认 JWT 密钥，生产应使用 `MOCK_EXTERNALS=false`。
- 已记录 Windows 签名 readiness 和生产阻塞检查命令。

验证结果：

- 文档旧口径扫描通过。
- Python 验收脚本编译通过。
- 本地 runtime readiness 通过：
  - `passed=true checks=15/18`
  - 仍提示真实 `llm.*` 与 `table.*` 配置缺失，这是当前预期阻塞。

当前结论：

- 本地测试继续推进不需要服务器。
- 上服务器前需要用户确认服务器、域名、HTTPS、外部 provider 白名单和代码签名证书。
- 如果继续不需要用户配合，可以下一步输出 Nginx/systemd/env/备份脚本模板。

下一步：

- 执行批次 A 侧边栏手测并填写报告。
- 或继续补部署模板文件。
