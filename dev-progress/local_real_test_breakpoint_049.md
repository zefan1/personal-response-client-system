# 断点 049：管理后台与侧边栏接口本地实操复测

时间：2026-07-10 12:05  
状态：已完成

## 本轮用户要求

- 用户浏览器已打开管理后台，侧边栏也在运行。
- 由 Codex 先自行操控电脑做一次本地真实测试。

## 已完成

- 使用 Chrome 接管 `http://127.0.0.1:5173/#/admin` 管理后台页面。
- 发现旧登录态会导致后台显示“运行模式未连接 / 网络连接失败”，退出后用 `admin/admin123` 重新登录，页面请求恢复正常。
- 逐个点击后台 A-L 模块并复查页面：
  - Skill 场景管理
  - 配置中心
  - 客户数据对接
  - 速搜内容管理
  - 账号与权限
  - 跟进规则引擎配置
  - 客户标签与分层
  - 运营分析看板
  - 版本管理
  - 系统公告
  - 操作审计日志
  - 系统健康监控
- 后台 A-L 页面复测结果：均可加载，无 `Failed to fetch`，无英文网络错误横幅。
- 跑通后台接口验收脚本：`python scripts\acceptance_admin_batch_b.py`，结果 `passed=true checks=39/39`。
- 跑通侧边栏接口验收脚本：`python scripts\acceptance_sidebar_batch_a.py`，修复后结果 `passed=true checks=15/15`。
- 修复侧边栏验收脚本跨天失效问题：运行前通过客户档案接口把 `18800003333` 的预约日期刷新为当天，避免“今日预约客户”样本过期。
- 修复后台系统公告状态口径：前端“生效中”筛选与统计从不存在的 `ACTIVE` 改为后端实际状态 `PUBLISHED`。
- 浏览器复查系统公告页面：`生效中=2`，筛选项值为 `PUBLISHED`。
- 复查速搜新增表单：
  - 默认支持“全部客资”。
  - 客户档案变量按钮存在。
  - 点击“客户昵称”后正文插入 `{{nickname}}`。
  - 切换“图片素材”后表单显示“图片说明/配套话术”和“图片地址”。
- 复查跟进规则新增表单：
  - 默认支持“全部客资”。
  - 优先级说明显示“数字越大越先执行”，并给出紧急 90、普通 60、观察 30 的建议。
- 验证公告 active 接口：`GET /api/v1/notices/active` 返回 2 条，包含用户发布的“测试”。
- 验证密码重置链路：
  - 临时管家旧密码重置前可登录。
  - 重置后旧密码返回 401。
  - 重置后旧 access token 返回 401。
  - 新密码可登录。
  - 临时账号已清理。
- 验证客户数据隔离：
  - 新建无客户的临时管家后，搜索 `1111` 返回 0 条。
  - 该管家今日跟进返回 0 条。
  - 管理员仍可搜索到 `1111`。
  - 临时账号已清理。

## 代码改动

- `scripts/acceptance_sidebar_batch_a.py`
  - 增加 A0 步骤：自动刷新今日预约样本日期。
- `desktop/src/renderer/modules/admin/AdminConsole.vue`
  - 系统公告“生效中”筛选值改为 `PUBLISHED`。
  - 系统公告“生效中”指标按 `PUBLISHED` 统计。

## 验证结果

- `python scripts\acceptance_admin_batch_b.py`：通过，39/39。
- `python scripts\acceptance_sidebar_batch_a.py`：通过，15/15。
- `npm test -- AdminConsole`：通过，23/23。
- `npm run typecheck`：通过。
- Chrome 管理后台 A-L 模块实操巡检：通过。

## 当前边界

- 侧边栏 Electron 独立窗口无法直接由 Chrome 自动化读取画面；本轮已通过侧边栏依赖接口验证公告、跟进、搜索、回复生成等数据链路。
- 系统健康页显示“表格通道 DOWN”，这是本地 mock/真实企微表格未接入状态，不影响本轮后台本地基础测试结论。
- 测试库仍有历史自动化残留数据和部分早期乱码夹具，后续可单独做测试数据清理。

## 下一步建议

- 用户在侧边栏肉眼确认工作台公告卡是否显示“测试”公告。
- 若侧边栏视觉也确认无误，进入真实 provider 配置前的清理与发布准备。
